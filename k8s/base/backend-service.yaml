apiVersion: v1
kind: Service
metadata:
  name: rust-agent-team-backend
  labels:
    app: rust-agent-team
    component: backend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4100
      protocol: TCP
      targetPort: 4100
  selector:
    app: rust-agent-team
    component: backend
