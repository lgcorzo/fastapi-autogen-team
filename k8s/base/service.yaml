apiVersion: v1
kind: Service
metadata:
  name: rust-agent-team
  labels:
    app: rust-agent-team
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4100
      protocol: TCP
      targetPort: 4100 # Ensure this matches the container's exposed port
  selector:
    app: rust-agent-team
