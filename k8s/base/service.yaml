apiVersion: v1
kind: Service
metadata:
  name: fastapi-autogen-team
  labels:
    app: fastapi-autogen-team
spec:
  type: ClusterIP
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8000  # Ensure this matches the container's exposed port
  selector:
    app: fastapi-autogen-team