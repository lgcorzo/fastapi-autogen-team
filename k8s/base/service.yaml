apiVersion: v1
kind: Service
metadata:
  name: fastapi-autogen-team
  labels:
    app: fastapi-autogen-team
spec:
  type: ClusterIP
  ports:
  - port: 4100
    protocol: TCP
    targetPort: 4100  # Ensure this matches the container's exposed port
  selector:
    app: fastapi-autogen-team